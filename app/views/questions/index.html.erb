<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h1>Preguntas</h1>

      <table class="table">
        <tr>
          <th>Título</th>
          <th>Descripción</th>
        </tr>
        <% @questions.each do |question| %>
          <tr>
            <td><%= question.title %></td>
            <td><%= question.description %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>